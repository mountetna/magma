Sequel.migration do
  change do
    create_table(Sequel[:<%= project_name.to_s %>][:projects]) do
      primary_key :id
      DateTime :created_at
      DateTime :updated_at
      String :description
      String :name
      unique :name
    end
  end
end
